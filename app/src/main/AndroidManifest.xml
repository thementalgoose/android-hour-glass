<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tmg.hourglass">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".HourGlassApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".dashboard.DashboardActivity"
            android:theme="@style/Launcher"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".modify.ModifyActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <activity android:name=".settings.SettingsActivity"
            android:exported="false" />

        <activity android:name=".settings.release.ReleaseActivity"
            android:exported="false" />

        <activity android:name=".settings.privacy.PrivacyPolicyActivity"
            android:exported="false" />

        <activity
            android:name="tmg.aboutthisapp.AboutThisAppActivity"
            android:exported="false"
            android:theme="@style/AppTheme.AboutThisApp" />

        <!-- Widget -->

        <!-- Bar Dark -->

        <activity
            android:name=".widget.bar.dark.WidgetBarDarkPickerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <receiver
            android:exported="false"
            android:name=".widget.bar.dark.WidgetBarDarkProvider"
            android:label="@string/widget_bar_label_dark">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_dark" />
        </receiver>

        <!-- Bar Light -->

        <activity
            android:exported="true"
            android:name=".widget.bar.light.WidgetBarLightPickerActivity">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <receiver
            android:exported="false"
            android:name=".widget.bar.light.WidgetBarLightProvider"
            android:label="@string/widget_bar_label_light">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_light" />
        </receiver>

        <!-- Circle Dark -->

        <activity
            android:exported="true"
            android:name=".widget.circle.light.WidgetCircleLightPickerActivity">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <receiver
            android:exported="false"
            android:name=".widget.circle.light.WidgetCircleLightProvider"
            android:label="@string/widget_circle_label_light">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_circle_light" />
        </receiver>

        <!-- Circle Light -->

        <activity
            android:exported="true"
            android:name=".widget.circle.dark.WidgetCircleDarkPickerActivity">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <receiver
            android:exported="false"
            android:name=".widget.circle.dark.WidgetCircleDarkProvider"
            android:label="@string/widget_circle_label_dark">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_circle_dark" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

    </application>

</manifest>