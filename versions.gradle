import groovy.json.JsonSlurper

private static def getLastNote() {
    def releaseNotes = "releases.json"
    def json = new File(releaseNotes)
    def parsedJson = new JsonSlurper().parseText(json.text)
    return parsedJson[parsedJson.size - 1]
}

private static def getVersionName() {
    return getLastNote().versionName as String
}

private static def getVersionCode() {
    return getLastNote().versionCode as Integer
}

ext {

    compileSdkVersion = 32
    targetSdkVersion = 32
    minSdkVersion = 23

    versionName = getVersionName()
    versionCode = getVersionCode()

    kotlin_version = "1.7.20"
    koin_version = "3.2.2"
    coroutines = "1.6.4"
    navigation = "2.5.1"
    mockk = "1.13.2"
    junit = "5.9.0"
    threeten = "1.4.0"
    threetenbp = "1.6.0"
    android_constraint = "2.1.4"
    android_compose = "1.3.0-rc01"
    android_compose_compiler = "1.3.2"
    android_material = "1.6.1"
    android_lifecycle = "2.5.1"
    android_splashscreen = "1.0.0"
    android_corektx = "1.8.0"
    android_appcompat = "1.5.1"
    android_accompanist = "0.26.5-rc"
    swipe_refresh = "1.1.0"

    tmg_utils = "2.4.2"
    tmg_about_this_app = "5.3.1"
    tmg_labelled_progressbar = "1.0.7"
    tmg_test_utils = "1.1.0"

    lottie = "5.2.0"
    firebase_bom = "31.0.0"
    retrofit = "2.9.0"
    okhttp3 = "4.7.2"
}